<ctrl:ThemedWindow
    x:Class="ClaudeAccountSwitcher.Views.ProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:ClaudeAccountSwitcher.Controls"
    xmlns:loc="clr-namespace:ClaudeAccountSwitcher.Localization"
    Title="{loc:Loc UpdTitle}"
    Width="400"
    SizeToContent="Height"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    Style="{DynamicResource ThemedWindowStyle}"
    >
    <StackPanel Margin="22 20">
        <TextBlock
            Text="{Binding Status}"
            TextWrapping="Wrap"
            Foreground="{DynamicResource WhiteBrush}"
            />
        <ProgressBar
            Height="8"
            Margin="0 14 0 8"
            Minimum="0"
            Maximum="100"
            Value="{Binding Progress}"
            Background="{DynamicResource CharcoalAshBrush}"
            Foreground="{DynamicResource AzureBrush}"
            BorderThickness="0"
            >
            <ProgressBar.Template>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4" SnapsToDevicePixels="True">
                        <Grid ClipToBounds="True">
                            <Border x:Name="PART_Track" />
                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="4" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </ProgressBar.Template>
        </ProgressBar>
        <Grid>
            <TextBlock
                Text="{Binding ProgressText}"
                VerticalAlignment="Center"
                Foreground="{DynamicResource SageMistBrush}"
                />
            <Button
                Content="{loc:Loc DlgCancel}"
                HorizontalAlignment="Right"
                MinWidth="84"
                Height="30"
                IsCancel="True"
                Click="Cancel_Click"
                />
        </Grid>
    </StackPanel>
</ctrl:ThemedWindow>
